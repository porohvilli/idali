// Initialize an empty array
let sources = [];

// Add elements to the array
sources.push('source1');
sources.push('source2');

// Access and use the array elements
console.log(sources[0]);  // Output: 'source1'
console.log(sources.length);  // Output: 2
